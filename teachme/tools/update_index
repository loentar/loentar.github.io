#!/bin/bash

cd ../db

for dir in $(find * -type d)
do
  (
    cd $dir
    for file in *
    do
      title=$(head -1 $file)
      echo "$file $title"
    done
    cd - &> /dev/null
  ) > $dir.index
done
