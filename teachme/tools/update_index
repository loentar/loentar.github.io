#!/bin/bash

cd ../db

echo -n >_index
for dir in $(find * -type d)
do
  cd $dir
  echo -n >_index
  for file in *
  do
    if [ "$file" != "_index" ]
    then
      title=$(head -1 $file)
      echo "$file $title" >>_index
    fi
  done
  cd -
  echo $dir >> _index
done
